describe("A huge f***ing suite with lots and lots and lots of tests", function() {
	for (let i = 0; i < 10000; i++) {
		it("should be loaded and run even if not only the number of tests is huge but also the test descriptions (and hence the test IDs) are very long #" + i, function() {});
	}
});
