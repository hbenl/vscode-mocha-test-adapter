{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "extensionHost",
      "request": "launch",
      "name": "Mocha adapter",
      "runtimeExecutable": "${execPath}",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}"
      ],
      "outFiles": [
        "${workspaceFolder}/out"
      ]
    },
    {
      "type": "node",
      "request": "attach",
      "name": "Mocha worker",
      "processId": "${command:PickProcess}"
    }
  ]
}
